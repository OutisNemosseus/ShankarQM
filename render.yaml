services:
  - type: web
    name: quantum-mechanics-notes
    runtime: static
    buildCommand: npm ci && npx quartz plugin install --from-config && npx quartz build
    staticPublishPath: ./public
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /*/index.html
